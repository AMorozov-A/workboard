# Local dev: leave unset — Vite proxies /api → http://localhost:3001
# VITE_API_BASE_URL=

# Production build: see .env.production (Vercel / npm run build)
# VITE_API_BASE_URL=https://workboard-vdp2.onrender.com/api
