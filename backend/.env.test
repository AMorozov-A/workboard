# Тестовая БД и секреты для Vitest (дублирует vitest.config.ts → test.env).
# Запуск тестов: из каталога backend/ — npm run test
DATABASE_URL="file:./prisma/test.db"
JWT_SECRET=test-jwt-secret-min-32-chars-long-for-testing!!
JWT_EXPIRES_IN=7d
